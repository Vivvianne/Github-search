export class Repository {
    public reponame: string;
    public repoDetails: string; 

    constructor(reponame: string, repoDetails: string){
        this.reponame = reponame, this.repoDetails = repoDetails

    }
}
